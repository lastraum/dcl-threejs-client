# NFT picture frames

Geometry sourced from [decentraland/unity-renderer](https://github.com/decentraland/unity-renderer)
`Assets/.../NFTShape/Meshes/*.fbx` (Explorer picture-frame styles).

Used at runtime via Three.js `FBXLoader` for SDK7 `NftShape` style parity.
